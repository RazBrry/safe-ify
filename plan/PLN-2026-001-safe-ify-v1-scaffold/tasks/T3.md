# T3 — Review scaffold + auth (S1 code)

**Plan:** PLN-2026-001
**Slice:** S1
**Role:** Code Quality Reviewer
**Model:** opus

## Context

Review all code produced in S1 (T1 scaffold + T2 auth commands) against the architecture and CLI specs. Verify security of config file handling, correctness of Cobra command structure, and TUI form implementation.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-001-safe-ify-v1-scaffold/CONTEXT.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/03-tech-spec-dev-architecture.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-cli-commands.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-config-permissions.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/05-tech-spec-dev-ops.md
- go.mod
- Makefile
- cmd/safe-ify/main.go
- internal/cli/root.go
- internal/cli/auth.go
- internal/config/types.go
- internal/config/global.go
- internal/config/project.go
- internal/config/errors.go
- internal/tui/styles.go
- internal/tui/forms.go

## Instructions

Review the implemented code against specs. Check each of the following:

1. **Project structure**: matches 03-arch section 1 layout. No misplaced files.
2. **go.mod**: correct module path, Go version 1.22+, required dependencies (cobra, huh, lipgloss, yaml.v3).
3. **Makefile**: has `build`, `clean`, `test`, `lint` targets per 05-ops section 1.1. LDFLAGS for version.
4. **Root command**: global flags (`--json`, `--config`, `--project`, `--version`) present per 04-cli section 1.
5. **Config types**: match 03-arch section 3 exactly (GlobalConfig, Instance, ProjectConfig, RuntimeConfig, etc.).
6. **Config security**: `CheckPermissions` correctly rejects >0600 files. `SaveGlobal` creates with 0600. Directory created with 0700.
7. **Project config traversal**: `FindProjectConfig` walks parent dirs per D10.
8. **Auth commands**: three subcommands (add, remove, list) registered under `auth` parent per 04-cli section 2.
9. **TUI forms**: use `huh` package correctly per D8. Token input masked. Instance name validated.
10. **Token validation**: `auth add` calls `/api/v1/healthcheck` with Bearer token.
11. **Token masking**: `auth list` masks token (first 4 + `****`).
12. **Error types**: all types from 04-config-permissions section 5 implemented with user-friendly messages.
13. **No secrets leaked**: token never logged or printed unmasked.

Output your verdict as PASS or FAIL with specific issues listed.

## Output

Verdict appended to Work Log.

## Acceptance Criteria

- [ ] Review covers all 13 check items above
- [ ] Verdict is PASS or FAIL with specific issues for any failures
- [ ] No blocking security issues in config handling

## Do NOT

- Modify any source code files
- Write test files
- Implement anything
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

### Subtasks Completed

### Test Results

### Conclusion

### Review Findings

**Reviewer:** Claude Opus (Code Quality Reviewer)
**Date:** 2026-03-10
**Scope:** S1 code — T1 scaffold + T2 auth commands

#### Checklist Results

| # | Check | Verdict | Notes |
|---|-------|---------|-------|
| 1 | Project structure matches 03-arch section 1 | PASS | `cmd/safe-ify/main.go`, `internal/cli/`, `internal/config/`, `internal/tui/`, `Makefile`, `go.mod` all present and correctly placed. S1 only covers scaffold + auth, so only the relevant subset of files exists. No misplaced files. |
| 2 | go.mod correct | PASS | Module path `github.com/erwinmaasbach/safe-ify` matches spec. Go version 1.26.1 (>= 1.22). All four required dependencies present: cobra v1.10.2, huh v1.0.0, lipgloss v1.1.0, yaml.v3 v3.0.1. Versions meet or exceed spec minimums. |
| 3 | Makefile targets | PASS | All four required targets present: `build`, `clean`, `test`, `lint`. LDFLAGS inject version via `-X main.version=$(VERSION)`. Also includes `test-coverage` target (bonus). Matches 05-ops section 1.1 exactly. |
| 4 | Root command global flags | PASS | All four required flags present: `--json` (bool), `--config` (string), `--project` (string), `--version` (bool). Version flag prints version and exits; no subcommand prints help. Matches 04-cli section 1. |
| 5 | Config types match 03-arch section 3 | PASS | `GlobalConfig`, `Instance`, `DefaultSettings`, `PermissionConfig`, `ProjectConfig`, `RuntimeConfig` all present with correct YAML tags and field types. Field names and types match spec exactly. |
| 6 | Config security | PASS | `CheckPermissions` rejects files with any group/other bits set (`& 0o077 != 0`), correctly enforcing <=0600. Also checks parent directory permissions (<=0700). `SaveGlobal` creates file with 0600, directory with 0700 via `os.MkdirAll`. |
| 7 | Project config traversal | PASS | `FindProjectConfig` walks parent directories in a loop using `filepath.Dir()`, stopping at filesystem root (`parent == dir`). Returns `ProjectConfigNotFoundError` if not found. Matches D10. |
| 8 | Auth commands | PASS | Three subcommands (`add`, `remove`, `list`) registered under `auth` parent command via `newAuthCmd()`. All added in `init()`. Matches 04-cli section 2. |
| 9 | TUI forms | PASS | Uses `huh.NewForm()` with `huh.NewInput()` and `huh.NewSelect()`. Token input uses `Password(true)` for masking. Instance name validated with regex `^[a-zA-Z0-9][a-zA-Z0-9-]*$`. URL validated with `url.ParseRequestURI`. Correctly uses `huh` per D8. |
| 10 | Token validation | PASS | `validateToken()` calls `GET /api/v1/healthcheck` with `Authorization: Bearer {token}` header. Handles 200/204 as success, 401 as auth failure, other codes as unexpected. |
| 11 | Token masking | PASS | `maskToken()` returns first 4 chars + `****`, or just `****` if token is <= 4 chars. Used in `auth list` output. Matches spec (first 4 + `****`). |
| 12 | Error types | PASS | All five error types from 04-config-permissions section 5 implemented: `ConfigInsecureError`, `ConfigNotFoundError`, `ProjectConfigNotFoundError`, `InstanceNotFoundError`, `PermissionDeniedError`. All implement `error` interface with user-friendly messages including remediation hints. |
| 13 | No secrets leaked | PASS | Token is never logged or printed unmasked. `auth list` uses `maskToken()`. `auth add` only passes token to `validateToken()` (HTTP request) and writes to config file. No `fmt.Print` of raw token anywhere. |

#### Additional Observations (non-blocking)

- `resolveConfigPath()` checks the `--config` flag but does not check `SAFE_IFY_CONFIG` environment variable as specified in 04-config-permissions section 1.1. This is a minor gap -- the env var fallback is missing. **Severity: Low** -- the `--config` flag provides equivalent functionality for overriding the path.
- `auth add` calls `os.Exit(1)` directly on token validation failure (line 135) instead of returning an error. This bypasses Cobra's error handling and could make testing harder. **Severity: Low** -- functional behavior is correct.
- `lipgloss` is used for styled output (styles.go) but `auth list` builds a custom table rather than using `lipgloss.Table`. This is fine for S1; the spec does not mandate a specific table implementation.

#### Verdict

**PASS**

All 13 checklist items pass. No blocking security issues in config handling. The two low-severity observations above are noted for potential future improvement but do not warrant a FAIL verdict.

### Codex Findings
