# T12 — Implement agent commands (deploy/redeploy/logs/status/list)

**Plan:** PLN-2026-001
**Slice:** S3
**Role:** Implementer
**Model:** sonnet

## Context

Implement all 5 agent-facing CLI commands. Per D6, all commands output a standard JSON envelope when `--json` is used. Each command checks permissions before making API calls. Per D5, `deploy` uses the deploy endpoint and `redeploy` uses the restart endpoint.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-001-safe-ify-v1-scaffold/CONTEXT.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-cli-commands.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/03-tech-spec-dev-architecture.md
- internal/cli/root.go
- internal/config/types.go
- internal/config/global.go
- internal/config/project.go
- internal/permissions/enforcer.go
- internal/coolify/client.go
- internal/coolify/types.go

## Instructions

1. Create `internal/cli/output.go`:
   - Define JSON envelope types per 03-arch section 5.1:
     ```go
     type Response struct {
         OK    bool        `json:"ok"`
         Data  interface{} `json:"data,omitempty"`
         Error *ErrorInfo  `json:"error,omitempty"`
     }
     type ErrorInfo struct {
         Code    string `json:"code"`
         Message string `json:"message"`
     }
     ```
   - `OutputJSON(w io.Writer, resp Response)` -- marshals with indent, writes to writer.
   - `OutputError(w io.Writer, code, message string)` -- creates error response, outputs.
   - Error codes: `PERMISSION_DENIED`, `CONFIG_NOT_FOUND`, `INSTANCE_NOT_FOUND`, `API_ERROR`, `NETWORK_ERROR`, `CONFIG_INSECURE`.

2. Create shared startup helper in `internal/cli/agent.go`:
   - `resolveAgentConfig(cmd *cobra.Command) (*config.RuntimeConfig, *coolify.Client, error)`:
     a. Find and load project config (with parent traversal).
     b. Load global config.
     c. Create RuntimeConfig via `config.ResolveRuntime()`.
     d. Create Coolify client.
     e. Return all three or error.
   - Implement `config.ResolveRuntime()` in config package if not already present (per 04-config-permissions section 3).

3. Create `internal/cli/deploy.go`:
   - Register `deploy` command. Flags: `--json`, `--force`.
   - Resolve config, check permission ("deploy"), call `client.Deploy()`, output result.

4. Create `internal/cli/redeploy.go`:
   - Register `redeploy` command. Flags: `--json`.
   - Resolve config, check permission ("redeploy"), call `client.Restart()`, output result.

5. Create `internal/cli/logs.go`:
   - Register `logs` command. Flags: `--json`, `--tail` (int, default 100).
   - Resolve config, check permission ("logs"), call `client.GetLogs()`, output result.

6. Create `internal/cli/status.go`:
   - Register `status` command. Flags: `--json`.
   - Resolve config, check permission ("status"), call `client.GetApplication()`, extract status fields, output.

7. Create `internal/cli/list.go`:
   - Register `list` command. Flags: `--json`, `--instance` (string, optional).
   - Note: `list` does NOT require project config. Uses `--instance` flag or first instance from global config.
   - Check permission ("list"), call `client.ListApplications()`, output result.

8. Each command: on permission denied, output error JSON (if `--json`) or text error, exit code 1.

9. Run `go build ./...` and verify success.

## Output

- internal/cli/output.go
- internal/cli/agent.go
- internal/cli/deploy.go
- internal/cli/redeploy.go
- internal/cli/logs.go
- internal/cli/status.go
- internal/cli/list.go

## Acceptance Criteria

- [ ] `go build ./...` succeeds
- [ ] All 5 commands registered and accessible via `safe-ify <cmd>`
- [ ] `--json` flag produces valid JSON envelope on all 5 commands
- [ ] Permission check runs before API call on every command
- [ ] Permission denied returns error with code `PERMISSION_DENIED`
- [ ] `deploy --force` passes force parameter to API
- [ ] `logs --tail 50` passes tail parameter to API
- [ ] `list` works without project config
- [ ] Exit code 1 on errors, 0 on success

## Do NOT

- Implement audit logging integration (that is T16)
- Implement doctor command (that is T17)
- Write test files (that is T14)
- Modify the coolify client package (T11)
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

### Subtasks Completed

### Test Results

### Conclusion

### Review Findings

### Codex Findings
