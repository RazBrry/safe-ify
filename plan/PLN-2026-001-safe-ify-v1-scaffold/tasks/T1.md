# T1 — Implement project scaffold + global config

**Plan:** PLN-2026-001
**Slice:** S1
**Role:** Implementer
**Model:** sonnet

## Context

This is the foundational task for the entire project. Create the Go project structure, Makefile, root Cobra command, and the global config loading/saving system with file permission security. Per D1, use Go + Cobra. Per D2, global config lives at `~/.config/safe-ify/config.yaml` with 0600 permissions. Per D9, use YAML with `gopkg.in/yaml.v3`.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-001-safe-ify-v1-scaffold/CONTEXT.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/03-tech-spec-dev-architecture.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-config-permissions.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/05-tech-spec-dev-ops.md

## Instructions

1. Initialize Go module: `go mod init github.com/erwinmaasbach/safe-ify` with Go 1.22+.
2. Create `Makefile` per 05-tech-spec-dev-ops.md section 1.1.
3. Create `cmd/safe-ify/main.go` as the entrypoint. It calls `cli.Execute()`.
4. Create `internal/cli/root.go`:
   - Define root Cobra command with `Use: "safe-ify"`.
   - Add global flags: `--json`, `--config`, `--project`, `--version`.
   - Wire `Execute()` function.
5. Create `internal/config/types.go` with all config struct types per 03-arch section 3 (`GlobalConfig`, `Instance`, `DefaultSettings`, `PermissionConfig`, `ProjectConfig`, `RuntimeConfig`).
6. Create `internal/config/global.go`:
   - `CheckPermissions(path)` -- validates file is 0600, directory is 0700.
   - `LoadGlobal(path)` -- checks permissions, reads YAML, unmarshals.
   - `SaveGlobal(path, cfg)` -- marshals YAML, creates dir with 0700, writes file with 0600.
   - `DefaultGlobalConfigPath()` -- returns `~/.config/safe-ify/config.yaml`.
7. Create `internal/config/project.go`:
   - `LoadProject(path)` -- reads and unmarshals project YAML.
   - `FindProjectConfig(startDir)` -- traverses parent directories looking for `.safe-ify.yaml` (per D10).
   - `SaveProject(path, cfg)` -- marshals and writes with 0644.
8. Create error types in `internal/config/errors.go` per 04-config-permissions section 5.
9. Run `go mod tidy` to fetch dependencies.
10. Verify `go build ./...` succeeds.
11. Verify `go vet ./...` passes.

## Output

- go.mod
- go.sum
- Makefile
- cmd/safe-ify/main.go
- internal/cli/root.go
- internal/config/types.go
- internal/config/global.go
- internal/config/project.go
- internal/config/errors.go

## Acceptance Criteria

- [ ] `go build ./...` succeeds with zero errors
- [ ] `go vet ./...` passes
- [ ] `make build` produces `./bin/safe-ify`
- [ ] `./bin/safe-ify --help` prints usage text
- [ ] GlobalConfig loads valid YAML and returns correct struct
- [ ] SaveGlobal creates file with 0600 permissions
- [ ] CheckPermissions rejects files with permissions more open than 0600
- [ ] FindProjectConfig traverses parent directories

## Do NOT

- Implement auth commands (that is T2)
- Implement permission enforcement logic (that is T6)
- Implement TUI forms (that is T2)
- Write test files (that is T4)
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

### Subtasks Completed

### Test Results

### Conclusion

### Review Findings

### Codex Findings
