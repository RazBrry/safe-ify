# T4 — Test scaffold + auth (S1 tests)

**Plan:** PLN-2026-001
**Slice:** S1
**Role:** Tester
**Model:** sonnet

## Context

Write and run unit tests for the config loading/saving system produced in T1 and the auth command logic from T2. Per 05-ops section 2.1, test config permission enforcement, load/save round-trips, and parent directory traversal.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-001-safe-ify-v1-scaffold/CONTEXT.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/05-tech-spec-dev-ops.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-config-permissions.md
- plan/PLN-2026-001-safe-ify-v1-scaffold/04-tech-spec-dev-cli-commands.md
- internal/config/types.go
- internal/config/global.go
- internal/config/project.go
- internal/config/errors.go
- internal/cli/auth.go
- internal/cli/root.go
- internal/tui/styles.go
- internal/tui/forms.go

## Instructions

1. Create `internal/config/global_test.go`:
   - `TestLoadGlobal_ValidConfig` -- create temp file with valid YAML, set 0600, load, verify struct.
   - `TestLoadGlobal_MissingFile` -- non-existent path, verify ConfigNotFoundError.
   - `TestLoadGlobal_InsecurePermissions` -- create temp file with 0644, verify ConfigInsecureError.
   - `TestSaveGlobal_CreatesWithCorrectPermissions` -- save to temp dir, stat file, verify 0600.
   - `TestSaveGlobal_CreatesDirectory` -- save to non-existent dir, verify dir created with 0700.

2. Create `internal/config/project_test.go`:
   - `TestLoadProject_ValidConfig` -- create temp file with valid project YAML, load, verify.
   - `TestFindProjectConfig_CurrentDir` -- create `.safe-ify.yaml` in temp dir, find from same dir.
   - `TestFindProjectConfig_ParentDir` -- create `.safe-ify.yaml` in parent, find from child dir.
   - `TestFindProjectConfig_NotFound` -- empty temp dir tree, verify ProjectConfigNotFoundError.

3. Create test fixtures in `testdata/`:
   - `testdata/valid-global-config.yaml` -- sample global config.
   - `testdata/valid-project-config.yaml` -- sample project config.

4. Run all tests: `go test ./internal/config/ -v -race`
5. Verify all tests pass.

## Output

- internal/config/global_test.go
- internal/config/project_test.go
- testdata/valid-global-config.yaml
- testdata/valid-project-config.yaml

## Acceptance Criteria

- [ ] All unit tests pass: `go test ./internal/config/ -v -race` exits 0
- [ ] Config permission test verifies 0600 enforcement
- [ ] Config permission test verifies rejection of 0644
- [ ] Parent directory traversal test passes
- [ ] Save creates directory and file with correct permissions
- [ ] Auth command registration verified: `auth add`, `auth remove`, `auth list` subcommands exist on root
- [ ] Token masking verified: `auth list` output shows first 4 chars + `****` (not full token)

## Do NOT

- Modify production code (only write test files)
- Test TUI interactions (those require manual testing)
- Write integration tests for the Coolify API (that is T14)
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

- `internal/config/global_test.go` (created)
- `internal/config/project_test.go` (created)
- `internal/config/testdata/valid-global-config.yaml` (created)
- `internal/config/testdata/valid-project-config.yaml` (created)
- `testdata/valid-global-config.yaml` (created)
- `testdata/valid-project-config.yaml` (created)

### Subtasks Completed

1. Created `internal/config/global_test.go` with 5 tests:
   - `TestLoadGlobal_ValidConfig` — creates temp file with valid YAML at 0600, loads, verifies struct.
   - `TestLoadGlobal_MissingFile` — non-existent path, verifies `ConfigNotFoundError`.
   - `TestLoadGlobal_InsecurePermissions` — temp file at 0644, verifies `ConfigInsecureError` with correct Path/Current/Expected.
   - `TestSaveGlobal_CreatesWithCorrectPermissions` — saves to temp dir, stats file, verifies 0600.
   - `TestSaveGlobal_CreatesDirectory` — saves to non-existent dir, verifies dir created at 0700 and file created.

2. Created `internal/config/project_test.go` with 4 tests:
   - `TestLoadProject_ValidConfig` — temp file with valid project YAML, loads, verifies all fields.
   - `TestFindProjectConfig_CurrentDir` — creates `.safe-ify.yaml` in temp dir, finds from same dir.
   - `TestFindProjectConfig_ParentDir` — creates `.safe-ify.yaml` in parent, finds from child dir.
   - `TestFindProjectConfig_NotFound` — empty temp dir tree, verifies `ProjectConfigNotFoundError`.

3. Created test fixture files at `testdata/` (project root) and `internal/config/testdata/`.

4. Verified auth command registration: `auth add`, `auth remove`, `auth list` are registered as subcommands of `auth` on root (`go run ./cmd/safe-ify/ auth --help` confirms).

5. Verified token masking: `maskToken` in `internal/cli/auth.go` returns `token[:4] + "****"` for tokens > 4 chars, or `"****"` for shorter tokens.

### Test Results

```
=== RUN   TestLoadGlobal_ValidConfig
=== PAUSE TestLoadGlobal_ValidConfig
=== RUN   TestLoadGlobal_MissingFile
=== PAUSE TestLoadGlobal_MissingFile
=== RUN   TestLoadGlobal_InsecurePermissions
=== PAUSE TestLoadGlobal_InsecurePermissions
=== RUN   TestSaveGlobal_CreatesWithCorrectPermissions
=== PAUSE TestSaveGlobal_CreatesWithCorrectPermissions
=== RUN   TestSaveGlobal_CreatesDirectory
=== PAUSE TestSaveGlobal_CreatesDirectory
=== RUN   TestLoadProject_ValidConfig
=== PAUSE TestLoadProject_ValidConfig
=== RUN   TestFindProjectConfig_CurrentDir
=== PAUSE TestFindProjectConfig_CurrentDir
=== RUN   TestFindProjectConfig_ParentDir
=== PAUSE TestFindProjectConfig_ParentDir
=== RUN   TestFindProjectConfig_NotFound
=== PAUSE TestFindProjectConfig_NotFound
=== CONT  TestLoadGlobal_MissingFile
=== CONT  TestLoadProject_ValidConfig
=== CONT  TestFindProjectConfig_ParentDir
=== CONT  TestLoadGlobal_InsecurePermissions
=== CONT  TestFindProjectConfig_CurrentDir
=== CONT  TestSaveGlobal_CreatesDirectory
=== CONT  TestSaveGlobal_CreatesWithCorrectPermissions
=== CONT  TestFindProjectConfig_NotFound
=== CONT  TestLoadGlobal_ValidConfig
--- PASS: TestLoadGlobal_MissingFile (0.00s)
--- PASS: TestLoadProject_ValidConfig (0.00s)
--- PASS: TestSaveGlobal_CreatesWithCorrectPermissions (0.00s)
--- PASS: TestFindProjectConfig_ParentDir (0.00s)
--- PASS: TestLoadGlobal_InsecurePermissions (0.00s)
--- PASS: TestFindProjectConfig_CurrentDir (0.00s)
--- PASS: TestFindProjectConfig_NotFound (0.00s)
--- PASS: TestSaveGlobal_CreatesDirectory (0.00s)
--- PASS: TestLoadGlobal_ValidConfig (0.00s)
PASS
ok  	github.com/erwinmaasbach/safe-ify/internal/config	1.391s
```

Command: `go test ./internal/config/ -v -race` — exit code 0.

### Conclusion

All 9 unit tests pass with `go test ./internal/config/ -v -race` (exit 0). All acceptance criteria are met:

- [x] All unit tests pass: exit 0
- [x] Config permission test verifies 0600 enforcement (`TestSaveGlobal_CreatesWithCorrectPermissions`)
- [x] Config permission test verifies rejection of 0644 (`TestLoadGlobal_InsecurePermissions`)
- [x] Parent directory traversal test passes (`TestFindProjectConfig_ParentDir`)
- [x] Save creates directory and file with correct permissions (`TestSaveGlobal_CreatesDirectory`)
- [x] Auth command registration verified: `auth add`, `auth remove`, `auth list` confirmed via `go run ./cmd/safe-ify/ auth --help`
- [x] Token masking verified: `maskToken` in `internal/cli/auth.go` returns first 4 chars + `****`

### Review Findings

### Codex Findings
