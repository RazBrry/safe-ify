# T10 — Test CLI + audit changes

**Plan:** PLN-2026-002
**Slice:** S2
**Role:** Tester
**Model:** Sonnet

## Context

Write and run tests for the CLI layer (--app flag, error codes, list bypass) and audit entry changes from T6, T7, T8. These tests exercise the integration between config resolution, CLI flags, and output.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-002-multi-app-support/CONTEXT.md
- plan/PLN-2026-002-multi-app-support/05-tech-spec-dev-ops.md
- internal/cli/root.go
- internal/cli/agent.go
- internal/cli/output.go
- internal/cli/deploy.go
- internal/cli/list.go
- internal/audit/types.go
- internal/audit/logger.go

## Instructions

1. **Create/update `internal/audit/types_test.go`** (or `entry_test.go`):
   - `TestEntry_StringWithAppName`: Create Entry with AppName="api", verify String() output includes "api" in the correct position.
   - `TestEntry_StringEmptyAppName`: Create Entry with empty AppName, verify String() handles it gracefully (empty field between pipes).

2. **Create/update CLI integration tests** (in appropriate `*_test.go` files under `internal/cli/`):

   These tests may need test fixtures (temp config files). Follow existing test patterns in the codebase.

   - `TestMapConfigError_AppNotFound`: Verify `mapConfigError` maps `AppNotFoundError` to `ErrCodeAppNotFound`.
   - `TestMapConfigError_AppAmbiguous`: Verify `mapConfigError` maps `AppAmbiguousError` to `ErrCodeAppAmbiguous`.

   If CLI integration tests are feasible (using Cobra test patterns):
   - `TestDeployCmd_AmbiguousApp`: Multi-app config, no `--app` flag, verify error response with APP_AMBIGUOUS code.
   - `TestListCmd_NoAppRequired`: Multi-app config, no `--app` flag, verify list command does not error with APP_AMBIGUOUS.

3. **Run tests:** `go test ./internal/audit/ ./internal/cli/`

4. **Run full suite:** `go test ./...`

## Output

- `internal/audit/types_test.go` (or `entry_test.go`)
- `internal/cli/agent_test.go` (or appropriate test file)

## Acceptance Criteria

- [ ] Audit Entry.String() tests exist and pass (with and without AppName)
- [ ] Error code mapping tests exist and pass
- [ ] `go test ./internal/audit/` passes
- [ ] `go test ./internal/cli/` passes
- [ ] `go test ./...` passes (no regressions)

## Do NOT

- Modify production code (only test files)
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

### Subtasks Completed

### Test Results

### Conclusion

### Review Findings

### Codex Findings
