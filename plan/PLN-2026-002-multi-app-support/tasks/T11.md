# T11 — Run full test suite and verify backward compat

**Plan:** PLN-2026-002
**Slice:** S2
**Role:** Tester
**Model:** Sonnet

## Context

Final verification that all tests pass and backward compatibility is maintained. This task runs the full test suite and specifically verifies that existing single-app config patterns (legacy format) work correctly end-to-end.

## Read

> Sub-agent: read ONLY the files listed here. Do not read any other files.
> If you need a file not listed, stop and report: DECISION NEEDED: [file] required but not listed.

- plan/PLN-2026-002-multi-app-support/CONTEXT.md
- plan/PLN-2026-002-multi-app-support/05-tech-spec-dev-ops.md

## Instructions

1. **Run the full test suite:**
   ```bash
   go test ./...
   ```
   Record the output.

2. **Run with race detector:**
   ```bash
   go test -race ./...
   ```
   Record the output.

3. **Verify build:**
   ```bash
   go build ./cmd/safe-ify
   ```

4. **Verify lint (if available):**
   ```bash
   make lint
   ```

5. **Manual backward-compat check:**
   - Create a temp directory
   - Write a legacy `.safe-ify.yaml` with `instance: test` and `app_uuid: abc-123`
   - Load it using a small Go test or by running the existing config test
   - Verify it normalizes correctly

6. Report results. If any test fails, report FAIL with details.

## Output

No new files. Results appended to Work Log.

## Acceptance Criteria

- [ ] `go test ./...` passes (all tests green)
- [ ] `go test -race ./...` passes (no race conditions)
- [ ] `go build ./cmd/safe-ify` succeeds
- [ ] Legacy config format loads correctly (verified by existing/new tests)

## Do NOT

- Modify production code
- Modify test code (only run existing tests)
- Read files not listed in the Read section above
- Modify TASKS.md, CONTEXT.md, or plan index files

---

## Work Log

> This section is appended by agents during execution. Static sections above this line are read-only.

### Files Touched

### Subtasks Completed

### Test Results

### Conclusion

### Review Findings

### Codex Findings
